const state={employeeCode:null,employeeName:null,mode:'SIGN_OUT',equipment:[],selected:new Map(),stream:null,scanTimer:null};const $=id=>document.getElementById(id);const equipmentCard=$('equipment-card'),successCard=$('success-card'),selectedAssets=$('selected-assets'),commitButton=$('commit-button'),scanner=$('scanner');function updateClock(){$('clock').textContent=new Intl.DateTimeFormat(undefined,{dateStyle:'medium',timeStyle:'short'}).format(new Date())}updateClock();setInterval(updateClock,30000);async function api(path,options){const r=await fetch(path,options),p=await r.json().catch(()=>({}));if(!r.ok)throw new Error(p.error||`Request failed (${r.status})`);return p}$('employee-form').addEventListener('submit',async e=>{e.preventDefault();const code=$('employee-code').value.trim();try{const result=await api(`/api/employees/lookup?code=${encodeURIComponent(code)}`);state.employeeCode=result.employeeCode;state.employeeName=result.name;$('employee-confirmation').textContent=`${result.employeeCode} — ${result.name}`;$('employee-confirmation').classList.remove('hidden');equipmentCard.classList.remove('hidden');state.equipment=(await api('/api/equipment')).equipment;renderSelected()}catch(error){$('employee-confirmation').textContent=error.message;$('employee-confirmation').classList.remove('hidden')}});document.querySelectorAll('.mode').forEach(button=>button.addEventListener('click',()=>{state.mode=button.dataset.mode;document.querySelectorAll('.mode').forEach(item=>item.classList.toggle('active',item===button));state.selected.clear();renderSelected()}));$('asset-form').addEventListener('submit',e=>{e.preventDefault();addAsset($('asset-code').value);$('asset-code').value='';$('asset-code').focus()});function normalizeCode(value){const code=value.trim().toUpperCase();return code.startsWith('EAS:')?code.slice(4):code}function addAsset(rawCode){const code=normalizeCode(rawCode);const asset=state.equipment.find(item=>item.asset_code===code||item.qr_code===rawCode.trim().toUpperCase());if(!asset)return setScanMessage(`Equipment code ${code||'(blank)'} was not found.`);const allowed=state.mode==='SIGN_OUT'?asset.status==='AVAILABLE':asset.status==='SIGNED_OUT';if(!allowed){const detail=asset.status==='SIGNED_OUT'&&asset.holder_name?` — currently held by ${asset.holder_name}`:'';return setScanMessage(`${asset.display_name} is ${asset.status.replace('_',' ').toLowerCase()}${detail}.`)}state.selected.set(asset.asset_code,asset);setScanMessage(`${asset.display_name} verified.`);renderSelected()}function renderSelected(){selectedAssets.replaceChildren();for(const asset of state.selected.values()){const row=document.createElement('div');row.className='asset';row.innerHTML=`<div><div><strong>${escapeHtml(asset.display_name)}</strong></div><span class="asset-code">${escapeHtml(asset.asset_code)}</span>${asset.unit_number?` · Unit ${escapeHtml(asset.unit_number)}`:''}</div>`;const remove=document.createElement('button');remove.type='button';remove.className='remove';remove.textContent='Remove';remove.addEventListener('click',()=>{state.selected.delete(asset.asset_code);renderSelected()});row.append(remove);selectedAssets.append(row)}const count=state.selected.size,signingOut=state.mode==='SIGN_OUT';$('review-title').textContent=signingOut?'Ready to sign out':'Ready to return';$('review-count').textContent=`${count} verified item${count===1?'':'s'}`;commitButton.textContent=signingOut?'SIGN OUT':'RETURN';commitButton.disabled=count===0||!state.employeeCode}commitButton.addEventListener('click',async()=>{commitButton.disabled=true;try{const endpoint=state.mode==='SIGN_OUT'?'/api/signout':'/api/return';const result=await api(endpoint,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({employeeCode:state.employeeCode,assets:[...state.selected.keys()].map(code=>({code})),terminalId:navigator.userAgent.slice(0,80)})});equipmentCard.classList.add('hidden');successCard.classList.remove('hidden');$('success-title').textContent=state.mode==='SIGN_OUT'?'Equipment signed out':'Equipment returned';$('success-detail').textContent=`${result.employee} · ${result.items.length} item${result.items.length===1?'':'s'} · ${new Date(result.occurredAt).toLocaleTimeString([],{hour:'numeric',minute:'2-digit'})}`;stopScanner()}catch(error){setScanMessage(error.message);commitButton.disabled=false}});$('new-transaction').addEventListener('click',()=>window.location.reload());$('scan-button').addEventListener('click',async()=>{if(state.stream)return stopScanner();if(!('BarcodeDetector'in window))return setScanMessage('QR scanning is not supported by this browser. Enter the printed asset code instead.');try{state.stream=await navigator.mediaDevices.getUserMedia({video:{facingMode:{ideal:'environment'}},audio:false});scanner.srcObject=state.stream;scanner.classList.remove('hidden');await scanner.play();$('scan-button').textContent='Stop Scanner';const detector=new BarcodeDetector({formats:['qr_code']});state.scanTimer=window.setInterval(async()=>{try{const codes=await detector.detect(scanner);if(codes[0]?.rawValue){addAsset(codes[0].rawValue);stopScanner()}}catch{}},350)}catch(error){setScanMessage(`Camera unavailable: ${error.message}`);stopScanner()}});function stopScanner(){if(state.scanTimer)clearInterval(state.scanTimer);state.scanTimer=null;state.stream?.getTracks().forEach(track=>track.stop());state.stream=null;scanner.srcObject=null;scanner.classList.add('hidden');$('scan-button').textContent='Scan QR'}function setScanMessage(message){$('scan-message').textContent=message}function escapeHtml(value){return String(value).replace(/[&<>'"]/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[char]))}
